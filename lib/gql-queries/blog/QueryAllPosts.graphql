query AllPosts {
  posts(first: 25, where: { orderby: { field: DATE, order: DESC } }) {
    nodes {
      date
      title
      excerpt
      link
      uri
      customExcerpt {
        excerpt
      }
    }
  }
}
