query AllPosts {
  posts(first: 1000, where: { orderby: { field: DATE, order: DESC } }) {
    nodes {
      date
      title
      excerpt
      link
      uri
      customExcerpt {
        excerpt
      }
    }
  }
}
